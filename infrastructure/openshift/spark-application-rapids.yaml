---
# RAPIDS GPU-Accelerated Feature Engineering Job
# Optional variant of smartshop-feature-engineering using RAPIDS JAR injection.
# Base image: docker.io/apache/spark:3.5.3 + rapids-4-spark 26.02.2 (cuda12).
# Requires GPU-equipped executor nodes with NVIDIA device plugin.
apiVersion: sparkoperator.k8s.io/v1beta2
kind: SparkApplication
metadata:
  name: smartshop-feature-engineering-rapids
  namespace: ${NAMESPACE}
spec:
  type: Python
  pythonVersion: "3"
  mode: cluster
  image: "${SPARK_RAPIDS_IMAGE}"
  imagePullPolicy: Always
  mainApplicationFile: "local:///opt/spark/work-dir/feature_engineering.py"
  arguments:
    - "--input"
    - "s3a://${S3_RAW_BUCKET}/raw/"
    - "--output"
    - "s3a://${S3_FEATURES_BUCKET}/"
    - "--metadata-input"
    - "s3a://${S3_RAW_BUCKET}/raw/metadata/"
  sparkVersion: "3.5.3"
  restartPolicy:
    type: OnFailure
    onFailureRetries: 2
    onFailureRetryInterval: 30
  hadoopConf:
    fs.s3a.endpoint: "http://minio.${NAMESPACE}.svc.cluster.local:9000"
    fs.s3a.path.style.access: "true"
    fs.s3a.impl: "org.apache.hadoop.fs.s3a.S3AFileSystem"
    fs.s3a.aws.credentials.provider: "org.apache.hadoop.fs.s3a.EnvironmentVariableCredentialsProvider"
  sparkConf:
    # RAPIDS Accelerator — plugin JAR pre-placed in /opt/spark/jars/ by the Containerfile
    spark.plugins: "com.nvidia.spark.SQLPlugin"
    spark.rapids.sql.enabled: "true"
    spark.rapids.sql.concurrentGpuTasks: "2"
    spark.rapids.memory.pinnedPool.size: "2g"
    spark.executor.memoryOverhead: "3g"
    # GPU resource allocation
    spark.executor.resource.gpu.amount: "1"
    spark.executor.resource.gpu.vendor: "nvidia.com"
    spark.task.resource.gpu.amount: "0.5"
    spark.executor.resource.gpu.discoveryScript: "/opt/spark/getGpusResources.sh"
    # JAR is in /opt/spark/jars/ (auto-loaded), but set extraClassPath as safety net
    spark.executor.extraClassPath: "/opt/spark/jars/rapids-4-spark.jar"
    spark.driver.extraClassPath: "/opt/spark/jars/rapids-4-spark.jar"
    # Adaptive query execution for GPU execution
    spark.sql.files.maxPartitionBytes: "512m"
    spark.sql.adaptive.enabled: "true"
  driver:
    cores: ${SPARK_DRIVER_CORES:-2}
    memory: "${SPARK_DRIVER_MEMORY:-4g}"
    serviceAccount: spark
    envFrom:
      - secretRef:
          name: smartshop-credentials
  executor:
    cores: ${SPARK_EXECUTOR_CORES:-4}
    instances: ${SPARK_EXECUTOR_INSTANCES:-4}
    memory: "${SPARK_EXECUTOR_MEMORY:-8g}"
    gpu:
      name: "nvidia.com/gpu"
      quantity: 1
    envFrom:
      - secretRef:
          name: smartshop-credentials
