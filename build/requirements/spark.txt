# Spark jobs image — feature engineering, text preprocessing, embedding generation
# Installed with: uv pip install --python python3.12 --no-cache -r spark.txt
# Note: pyspark pinned to match sparkVersion in spark-application.yaml
pyspark==3.5.3
feast==0.62.0
pandas==2.2.3
pyarrow==18.1.0
sentence-transformers==3.3.1
fsspec==2024.12.0
s3fs==2024.12.0
datasets==3.2.0
huggingface_hub==0.27.1
