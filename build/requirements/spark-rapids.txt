# RAPIDS spark image — feature engineering only (no Feast, Python 3.8 compatible)
# Base image apache/spark:3.5.3 ships Python 3.8 (Ubuntu 20.04 focal).
# Feast >=0.60 requires Python 3.9+ so it's intentionally excluded here.
# Feast materialization runs separately in the main spark-jobs image (UBI9/Python 3.12).
pyspark==3.5.3
pandas>=2.0.0,<2.2.0
pyarrow>=14.0.0,<15.0.0
fsspec>=2024.2.0
s3fs>=2024.2.0
