# Training image — deps installed on top of the RHOAI official base image.
# Base image (quay.io/opendatahub/odh-th06-cuda130-torch291-py312:odh-3.4) already includes:
#   torch, pandas, pyarrow, s3fs, fsspec, mlflow (3.10.1+rhaiv.2), datasets,
#   transformers, peft, trl, bitsandbytes, accelerate, sentencepiece, deepspeed,
#   flash-attn, xformers, numpy, scipy, scikit-learn, aiohttp, aiobotocore
#
# Only packages NOT in the base:
pyspark==4.0.0
feast==0.62.0
